"use strict"; // <-- Syntax, and treat all JavaScript Code as a newer version of JavaScript Code.

// alert("hello") // [alert()] This will work on Browser, not here(Node.js)
// alert(3 + 3) // Arithmetic operation

// console.log(3 + 3) console.log("Fahim") // Do Not use

console.log(3
     + 
     3) // Do Not use // code readability should be high(not use)

console.log(3 + 3) // (use this)

console.log("Fahim") // (use this)


//JavaScript (JS) DataTypes:
let name = "Fahim" // String
let age = 18 // Number
let isLoggedIn = false // Boolean


/* Primitive DataTypes : 
1. number => 2 to power 53(bigint),
2. bigint => very big number(especially trading, stock market, reddit, etc.))
2. string => "" ("" or ''),
3. boolean => true/false (user loggedIn yes or no, credit card details available yes or no, server response yes or no, etc.),
4. null => standalone value (empty value, like let price = null(it's not undefined, it's only empty(ex: server send null value as a temperature if server not working. ))),
5. undefined => let state; (declared a variable but not assigned any value.)
6. symbol (unique(React.js))
*/



//JavaScript (JS) Object(type):

console.log(typeof "Fahim"); // string
console.log(typeof age); // number
console.log(typeof null); // object // Interview Question
console.log(typeof undefined); // undefined


